# sovernity.com

Static site for **Sovernity Chat**, and the host for its release download.

> ## Generated — do not edit this repository by hand
>
> Every file here is copied from `website/` in the private application
> repository by `scripts/publish-website.sh`, which refuses to publish unless
> the privacy policy matches the copy inside the shipped build, the ban-list
> grep is clean, the source-available section is still gated, and the download
> link does not point at a private repository.
>
> **Edit the private repo and re-run that script.** A change made here is lost
> on the next publish — and worse, it would be a claim that never passed the
> checks, on the surface where claims matter most.

Published from source commit `780e8d5`.

No cookies, no analytics, no tracking — these are static files.
